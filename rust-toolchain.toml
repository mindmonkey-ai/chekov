# §D.7: the toolchain pin is the single source of truth; CI reads this file.
# 1.95.0 is the stable installed on the target machine (edition 2024 needs >=1.85).
[toolchain]
channel = "1.95.0"
components = ["clippy", "rustfmt"]
